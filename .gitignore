# ── OS & editors ─────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo

# ── VS Code — ignore personal/generated files, keep shared project settings ──
.vscode/*
!.vscode/settings.json

# ── Environment & secrets ────────────────────────────────────
.env
.env.*
!.env.example
!.env.prod.example

# ── Node.js ──────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-error.log*
*.log

# ── Frontend build output ────────────────────────────────────
pwa/dist/
pwa/.vite/
pwa/coverage/
pwa/cypress/screenshots/
pwa/cypress/videos/
pwa/.ionic/
pwa/android/
pwa/ios/

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
.pytest_cache/
htmlcov/
.coverage
*.egg-info/
.ipynb_checkpoints/
*.ipynb

# ── Database & persistent storage ────────────────────────────
.persist/
# Scratch dir for the local prod-compose smoke test (stand-in for the
# on-box /srv/apps/<app>/public mount) — never committed.
/public/

# ── Personal CV seed (schema/template stay in 03-init-cv.sql; this holds
#    the real CV content — name, contacts, publications) ──────────────────
init-scripts/seed-cv-samples.sql

# ── Claude Code ──────────────────────────────────────────────
# Ignore personal/machine config (settings.local.json, etc.), but share
# the curated memory knowledge base, the path-gated rules, and the skills
# so others can grasp and work the system via Claude Code.
.claude/*
!.claude/memory/
!.claude/rules/
!.claude/skills/
!.claude/agents/
# ...except personal notes, personal infra, the brainstorm ledger, and the
# unfixed-security-findings TODO
.claude/memory/user-manuel.md
.claude/memory/deployment-plan.md
.claude/memory/production-walkthrough.md
.claude/memory/idea-ledger.md
# NOTE: upstream gitignores .claude/memory/pre-deployment-todo.md (its copy is
# personal infra). This fork deliberately tracks its own — the audit findings are
# shared, not local — so that line is intentionally NOT taken from upstream.

# ── Local assets ─────────────────────────────────────────────
# Personal CV source dropped into pwa/public for hand-testing the PDF
# viewer; nothing in the app references it and it is not a seeded asset
# (only *.png under pwa/public is scanned into MinIO on boot).
pwa/public/Profile.pdf
